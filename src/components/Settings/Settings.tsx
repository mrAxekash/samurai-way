import React from "react";
import s from './Settings.module.css'

export const Settings = () => {

    return (
        <div className={s.header}>
            <h2>My Settings</h2>
        </div>
    )
}